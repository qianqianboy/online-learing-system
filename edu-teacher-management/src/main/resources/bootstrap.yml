spring:
  application:
    name: edu-teacher-management
  cloud:
      nacos:
        config:
          server-addr: 127.0.0.1:8848
          file-extension: yaml
  profiles:
    active: dev
server:
  #服务端口号：8100
  port: 8100